# In-repo, cross-package deps stay as workspace:* ; changesets rewrites them
# to real semver ranges at publish time.
save-workspace-protocol=rolling
# Peers (react, firebase-admin) are declared explicitly; don't auto-install.
auto-install-peers=false
