---
"@validation-os/core": patch
"@validation-os/dashboard": patch
"@validation-os/adapter-firestore": patch
"@validation-os/api": patch
---

UX fixes: remove backend/agent header indicators, consistent next-move box sizes (equal columns + equal-height items), grid text breathing room (wider cell spacing, taller cells, bigger text), donut chart rewritten as proper ring, merged reading/experiment detail belief cards (no duplicate sections), needs-framing now per-lens (top 3, one per lens), grading rationale labeled correctly (not fake quotes), "see context above" link for actual quotes.
