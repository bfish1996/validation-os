# The code monorepo lives under packages/. The markdown registry, skills,
# docs, and Python validators are not workspace members — they ship as agent
# skills via skills.sh (`npx skills`), not npm.
packages:
  - "packages/*"
